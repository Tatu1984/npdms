/** Roles and permissions — what each job in the force is allowed to do. */
export const rolesEn = {
  nav: {
    name: "Roles and permissions",
    desc: "What each job is allowed to do, as distinct from how senior it is",
  },
} as const;
